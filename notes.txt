download libraries
1) express
2) mongoose
3) cors
4) dotenv
5) bcryptjs
6) nodemailer
7) jsonwebtoken
8) zod

    npm install express mongoose cors dotenv bcryptjs nodemailer jsonwebtoken zod

CI/CD Pipeline with DevOps
**************************
1) launch EC2 instance

2) open below ports
    8080 -- Jenkins
    80 -- Docker
    5000 -- NodeJS appln

3) connect to EC2

4) update ubuntu
    > sudo apt update -y

5) install jdk
    Ex.
        https://www.jenkins.io/doc/book/installing/linux/

    sudo apt update
    sudo apt install fontconfig openjdk-21-jre
    java -version

6) install jenkins   

7) install docker
    sudo apt install docker.io -y
    docker --version

8) install NodeJS
    sudo apt update
    sudo apt install curl build-essential -y

    curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash

    export NVM_DIR="$HOME/.nvm"
    source "$NVM_DIR/nvm.sh"
    source "$NVM_DIR/bash_completion"

    nvm --version

    nvm install 24.13.0

    nvm use 24.13.0
    nvm alias default 24.13.0

    node -v
    npm -v

9) enable permissions
    sudo usermod -aG docker ubuntu
    sudo usermod -aG docker jenkins
    sudo usermod -aG jenkins ubuntu
    sudo systemctl restart jenkins
    sudo reboot

10) check status of jenkins and docker
    sudo systemctl status jenkins
    sudo systemctl status docker


11) get the jenkins password
    sudo cat /var/lib/jenkins/secrets/initialAdminPassword

12) connect to jenkins and install plugins

        https://34.236.153.44:8080/

        1) GitServer
        2) DockerPipeline
        3) Docker

13) ADD Global Credentials
    MONGO_URL=mongodb+srv://admin:admin@careerit.uqkohdy.mongodb.net/ecomm_db?appName=CareerIT
    PORT=5000
    EMAIL=samba.pendela@gmail.com
    PASSWORD=lbcw zyys ejzu jrcr
    JWTSECRETE=secrete123
    EXPIRES_IN=1d


